                      ),
                    ),
                    icon: const Icon(Icons.share_rounded, size: 15),
                    label: const Text('Ù…Ø´Ø§Ø±ÙƒØ© Ø§Ù„Ø¯Ø¹ÙˆØ© ÙƒØ§Ù…Ù„Ø©', style: TextStyle(fontSize: 12, fontWeight: FontWeight.bold)),
                  ),
                ),
                const SizedBox(width: 10),
                Expanded(
                  child: OutlinedButton.icon(
                    onPressed: () {
                      Navigator.of(ctx).pop();
                      _shareOnlyRoomCode(room.id);
                    },
                    style: OutlinedButton.styleFrom(
                      foregroundColor: const Color(0xFFFFD700),
                      side: BorderSide(color: const Color(0xFFFFD700).withValues(alpha: 0.4)),
                      padding: const EdgeInsets.symmetric(vertical: 10),
                      shape: RoundedRectangleBorder(
                        borderRadius: BorderRadius.circular(10),
                      ),
                    ),
                    icon: const Icon(Icons.send_rounded, size: 15),
                    label: const Text('Ø¥Ø±Ø³Ø§Ù„ Ø§Ù„Ø±Ù…Ø² ÙÙ‚Ø·', style: TextStyle(fontSize: 12, fontWeight: FontWeight.bold)),
                  ),
                ),
              ],
            ),
          ],
        ),
      ),
    );
  }

  Future<bool> _confirmLeave() async {
    if (_leaving) return false;
    final userId = context.read<AppState>().user?.uid;
    final isSpectator = widget.isSpectator ||
        userId == null ||
        (_latestRoom != null && !_latestRoom!.containsPlayer(userId));
    if (isSpectator) {
